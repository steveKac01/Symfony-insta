{% extends "base.html.twig" %}
{% block title %}
	{{parent()}}
	- Register
{% endblock %}
{% block content %}
	<div class="container my-5 form-control">

		<h1>
			Register
		</h1>

		{{ form_start(form) }}
		{{ form_widget(form) }}
		{{ form_end(form) }}
		<p class="mt-4 text-end">Already got an account ? <a href="{{path('security.login')}}">sign in here</a> </p>

	</div>
{% endblock %}
