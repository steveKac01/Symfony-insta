<a href="{{path('post.user',{id:userId})}}">
    <img src="/images/avatar/{{avatar?avatar:'default.jpg'}}" class="border" width="50px" height="50px" alt="user avatar" class="mt-4">
</a>
<p>By
    <a href="{{path('post.user',{id:userId})}}" class="text-white text-decoration-none">
        <strong>{{pseudo|capitalize}}</strong>
    </a>on
    {{createdAt|date('m/d/Y')}}
    at
    {{createdAt|date('H:i')}}</p>