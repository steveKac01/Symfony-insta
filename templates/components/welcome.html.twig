	<div class="jumbotron">
        <h1 class="display-4">Welcome to instaPic !</h1>
        <p class="lead">InstaPIC is an app to simply and quickly share your images !</p>
        <hr class="my-4">
        <p>To start posting,
            <a href="{{path('user.register')}}">register</a>
            or
            <a href="{{path('security.login')}}">log</a>
            into your account.</p>
        <a class="btn btn-primary btn-lg" href="{{path('user.register')}}" role="button">Register</a>
        <a class="btn btn-primary btn-lg" href="{{path('security.login')}}" role="button">Login</a>
    </div>