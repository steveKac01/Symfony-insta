<header>

	<nav class="navbar navbar-expand-lg navbar-dark bg-primary py-4" id="top">
		<div class="container-fluid">
			<a class="navbar-brand" href="{{path('home')}}">INSTAPIC</a>
			<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
			<div class="collapse navbar-collapse" id="navbarColor01">
				<ul class="navbar-nav me-auto">
					<li class="nav-item">
						<a class="nav-link active" href="{{path('home')}}">
							<i class="fa-solid fa-house"></i>
							Home
							<span class="visually-hidden">(current)</span>
						</a>
					</li>

					{# Posts #}
					{% if app.user %}
						<li class="nav-item dropdown">
							<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Posts</a>
							<div class="dropdown-menu" bis_skin_checked="1">
								<a class="dropdown-item" href="{{path('post.new')}}">
									<i class="fa-solid fa-plus"></i>
									Add post</a>
								<a class="dropdown-item" href="{{path('post.user',{id:app.user.id})}}">
									<i class="fa-solid fa-list"></i>
									My posts</a>

							</li>

						{% endif %}

						{# Account #}
						{% if app.user %}
							<li class="nav-item dropdown">
								<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
									<i class="fa-solid fa-user pe-1"></i>
									Account</a>
								<div class="dropdown-menu" bis_skin_checked="1">
									<a class="dropdown-item" href="{{path('user.update',{id:app.user.id})}}">Profile</a>
									<a class="dropdown-item" href="{{path('user.changePassword',{id:app.user.id})}}">Change Password</a>
									<div class="dropdown-divider" bis_skin_checked="1"></div>
									<a class="dropdown-item" href="{{path('security.logout')}}">
										<i class="fa-solid fa-right-from-bracket"></i>
										Logout</a>
								</div>
							</li>
						{% else %}

							<li class="nav-item">
								<a class="nav-link" href="{{path('security.login')}}">
									Sign-in</a>
							</li>

							<li class="nav-item">
								<a class="nav-link" href="{{path('user.register')}}">
									Register</a>
							</li>
						{% endif %}
						<li class="nav-item">
							<a class="nav-link" href="{{path('contact')}}">
								<i class="fa-solid fa-message"></i>
								Contact</a>
						</li>

					</ul>

					{# Search form #}
					<form class="d-flex" action="{{path('home')}}" method="POST">
						<input class="form-control me-sm-2" type="text" placeholder="Search" minlength="3" maxlength="15" id="search" name="search">
						<button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
					</form>

				</div>
			</div>
		</nav>

	</header>
