<header>

	<nav class="navbar navbar-expand-lg navbar-dark bg-primary py-4" id="top">
		<div class="container-fluid">
			<a class="navbar-brand" href="{{path('home')}}">INSTAKILO</a>
			<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
			<div class="collapse navbar-collapse" id="navbarColor01">
				<ul class="navbar-nav me-auto">
					<li class="nav-item">
						<a class="nav-link active" href="{{path('home')}}">Home
							<span class="visually-hidden">(current)</span>
						</a>
					</li>
					{% if app.user %}
					<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Posts</a>
						<div class="dropdown-menu" bis_skin_checked="1">
							<a class="dropdown-item" href="{{path('post.user')}}">My posts</a>
							<a class="dropdown-item" href="{{path('image.new')}}">Add post</a>
					</li>

					{% endif %}
					<li class="nav-item">
						<a class="nav-link" href="{{path('privacy')}}">About</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{path('contact')}}">Contact</a>
					</li>

					{% if app.user %}
						<li class="nav-item dropdown">
							<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Account</a>
							<div class="dropdown-menu" bis_skin_checked="1">
								<a class="dropdown-item" href="{{path('user.update',{id:app.user.id})}}">Profile</a>
								<a class="dropdown-item" href="{{path('user.changePassword',{id:app.user.id})}}">Change Password</a>
								<div class="dropdown-divider" bis_skin_checked="1"></div>
								<a class="dropdown-item" href="{{path('security.logout')}}">Logout</a>
							</div>
						</li>
					{% else %}

						<li class="nav-item">
							<a class="nav-link" href="{{path('security.login')}}">
								Sign-in</a>
						</li>

						<li class="nav-item">
							<a class="nav-link" href="{{path('user.register')}}">
								Register</a>
						</li>
					{% endif %}


				</ul>

			</div>
		</div>
	</nav>

</header>
