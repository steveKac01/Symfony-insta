{{ form_start(form) }}

{# Password #}
{% include "form/templates/field.html.twig" with {field:form.plainPassword} only %}

{# Repeat Password #}
{% include "form/templates/repeat_password.html.twig" with {field:form.newPassword} only %}


{{ form_end(form) }}
